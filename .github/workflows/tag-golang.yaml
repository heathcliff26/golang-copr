---
name: "Tag Golang"

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "golang.spec"


jobs:
  tag:
    uses: heathcliff26/ci/.github/workflows/run-script.yaml@main
    permissions:
      contents: write
    with:
      cmd: hack/tag.sh golang
